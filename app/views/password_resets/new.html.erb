<h1>Forgot Password</h1>
<%= form_for @user, url: password_resets_path do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit 'Send Request' %>
  </div>
<% end %>
